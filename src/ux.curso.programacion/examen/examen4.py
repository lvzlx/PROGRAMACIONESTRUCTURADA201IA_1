#Ejercicio 4
#Entrada de datos: Se solicita el peso y el valor de entrada

w = float(input("Ingresa el peso (w): "))
x = float(input("Ingresa el dato de entrada (x): "))


#Proceso del cálculo de activación: se aplica la fórmula Z = w * x

Z = w * x


#Salida de resultado: se muestra el valor calculado

print("El valor de activación Z es:", Z)